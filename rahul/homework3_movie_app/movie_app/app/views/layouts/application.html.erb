<!DOCTYPE html>
<html>
<head>
  <title>MovieApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<!-- Nav Bar Stuff -->
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Retflix', root_path, class: 'navbar-brand' %>

       <!-- Collect the nav links, forms, and other content for toggling -->
      <ul class="nav navbar-nav">
        <li><%= link_to 'Show All Movies', search_index_path%></li>
      </ul>

       <!-- This bit is a bit of trial for the search -->
      <%= form_tag(search_index_path, :method => "get", id: "search-form", class: "navbar-form navbar-left") do %>
        <%= text_field_tag :search, params[:search], placeholder: "Search Movie Titles", class: 'form-control' %>
        <%= submit_tag "Search", :name => nil , class: 'btn btn-default' %>
      <% end %>
    </div>
  </div>
</nav>

<div class="container-fluid">

    <%= render 'shared/flash' %>
    <%= yield %>

</div>

</body>
</html>
